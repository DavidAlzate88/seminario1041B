spring.application.name=seminario1041B

server.servlet.context-path=/api/compras
server.port=0

#settings: Database
spring.datasource.url=${optional:DB_DATABASE:jdbc:h2:mem:testdb}
spring.datasource.username=${optional:DB_USER:sa}
spring.datasource.password=${optional:DB_PASSWORD:password}
